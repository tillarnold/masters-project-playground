[package]
name = "opendp-test-project"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
csv = "1.2.2"
opendp = { path = "../opendp/rust", features = ["untrusted", "partials"] }
